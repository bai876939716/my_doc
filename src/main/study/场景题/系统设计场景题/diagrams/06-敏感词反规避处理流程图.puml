@startuml 敏感词反规避处理流程图
title 敏感词反规避处理流程图

start
:原始文本输入;
:去除分隔符和空白字符做归一化;
:繁体转简体;
:形近字归一化替换为等价代表字符;

fork
  :精确AC自动机匹配;
fork again
  :转换为拼音序列;
  :拼音AC自动机匹配;
end fork

if (精确通道是否命中?) then (是)
  :直接拦截或替换为星号;
  stop
else (否)
  if (拼音通道是否命中?) then (是)
    :标记为疑似违规\n转人工复核或额外规则确认;
    stop
  else (否)
    :放行;
    stop
  endif
endif

@enduml
